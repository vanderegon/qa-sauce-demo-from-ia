{
  "name": "qa-sauce-demo-from-ia",
  "version": "0.1.0",
  "description": "QA E2E tests for saucedemo.com generated by an n8n AI Agent + Playwright MCP",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://www.saucedemo.com"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.12.0",
    "typescript": "^5.5.0"
  }
}
